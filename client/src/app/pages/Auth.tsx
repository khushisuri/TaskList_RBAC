import { Box } from '@mui/material';
import React from 'react';
import Form from '../components/Form';

const Auth = () => {
  return (
    <Box>
      <Form />
    </Box>
  );
};

export default Auth;
